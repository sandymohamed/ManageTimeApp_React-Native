diff --git a/node_modules/react-native-alarm-notification/android/build.gradle b/node_modules/react-native-alarm-notification/android/build.gradle
index 76a290f..42fbac5 100644
--- a/node_modules/react-native-alarm-notification/android/build.gradle
+++ b/node_modules/react-native-alarm-notification/android/build.gradle
@@ -20,7 +20,6 @@ def safeExtGet(prop, fallback) {
 }
 
 apply plugin: 'com.android.library'
-apply plugin: 'maven'
 
 buildscript {
     // The Android Gradle plugin is only required when opening the android folder stand-alone.
@@ -30,19 +29,16 @@ buildscript {
     if (project == rootProject) {
         repositories {
             google()
-            jcenter()
+            mavenCentral()
         }
         dependencies {
-            classpath 'com.android.tools.build:gradle:3.4.1'
+            classpath 'com.android.tools.build:gradle:7.4.2'
         }
     }
 }
 
-apply plugin: 'com.android.library'
-apply plugin: 'maven'
-
 android {
-    compileSdkVersion safeExtGet('compileSdkVersion', DEFAULT_COMPILE_SDK_VERSION)
+    compileSdk safeExtGet('compileSdkVersion', DEFAULT_COMPILE_SDK_VERSION)
     buildToolsVersion safeExtGet('buildToolsVersion', DEFAULT_BUILD_TOOLS_VERSION)
     defaultConfig {
         minSdkVersion safeExtGet('minSdkVersion', DEFAULT_MIN_SDK_VERSION)
@@ -67,7 +63,7 @@ repositories {
         url "$rootDir/../node_modules/jsc-android/dist"
     }
     google()
-    jcenter()
+    mavenCentral()
 }
 
 dependencies {
@@ -111,7 +107,12 @@ afterEvaluate { project ->
     task androidJavadoc(type: Javadoc) {
         source = android.sourceSets.main.java.srcDirs
         classpath += files(android.bootClasspath)
-        classpath += files(project.getConfigurations().getByName('compile').asList())
+        // Use runtimeClasspath which is resolvable (canBeResolved=true)
+        // This extends implementation but can be resolved
+        def runtimeClasspath = project.getConfigurations().findByName('runtimeClasspath')
+        if (runtimeClasspath != null && runtimeClasspath.canBeResolved) {
+            classpath += files(runtimeClasspath)
+        }
         include '**/*.java'
     }
 
